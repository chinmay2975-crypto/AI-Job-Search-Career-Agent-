GROQ_API_KEY=
SERPER_API_KEY=
DRY_RUN=true
AUTO_SUBMIT_DAILY_CAP=5
MATCH_SCORE_THRESHOLD=70
# Optional: SQLite database location (default data/career_agent.db; relative paths are from the project root)
SQLITE_DB_PATH=
# Where the synthetic sandbox submitter posts; change if the backend isn't on port 8000
SANDBOX_APPLY_URL=http://localhost:8000/sandbox/apply
